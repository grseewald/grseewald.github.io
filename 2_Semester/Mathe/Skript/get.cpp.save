#include<iostream>

int main(){
	for int(i=1;i<13,i++){
		string str = "string str = "gcc "; 
    str = str + " -o a.out " + filename;string str = "gcc "; 
    str = str + " -o a.out " + filename;"; 
    str = str + " -o a.out " + filename;
		system(wget get https://ifm.mathematik.uni-wuerzburg.de/\~dobro/inf2/kap.pdf)};
	}
}
